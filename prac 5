import sympy as sp

x,lam = sp.symbols('x lam',nonnegative = True)

f = x**8

g = 4-x

L = f+lam*g

eq1 =  sp.diff(L,x)
eq2 = lam*g

solutions = sp.solve((eq1,eq2),(x,lam),dict=True)

print("Possible Solutions:")
print(solutions)

for sol in solutions:
  x_val = sol[x]
  lam_val = sol[lam]

  if x_val>=4 and lam_val>=0:
    print("\nOptimal Solution")
    print("x = ",x_val)
    print("Lamda = ",lam_val)
    print("Minimum Vlaue = ",f.subs(x,x_val))
